# Allot

Allot is an in-progress, highly experimental language runtime.

### How to Run

First install it using cargo install:
```shell
cargo install allot
```

Then to see options:
```shell
allot --help
```
